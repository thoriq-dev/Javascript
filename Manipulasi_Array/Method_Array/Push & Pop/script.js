let array = [];

array = ["Thoriq", "Micko", "Alif", "Rizqo"];

// array.push("Ahmad");

for (i = 0; i < array.length; i++) {
  console.log("Nama Mahasiswa ke-" + (i + 1) + " : " + array[i]);
}

array.pop();
